// What the landing page shows in small print, and what tells you at a glance
// whether the bundle you are looking at is the one that was last published.
// A deploy that silently served a stale bundle is the reason this exists.
export const APP_VERSION = '0.1.0'
